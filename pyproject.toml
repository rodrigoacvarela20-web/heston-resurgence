[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ftfinance"
version = "1.0.0"
description = "Exploratory resurgent-asymptotic analysis of rare-event tails in the Heston model"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "matplotlib>=3.8",
    # The fold solver is verified with mpmath 1.3; 1.4 changes the real sqrt
    # branch behaviour on its continuation and currently fails three tests.
    "mpmath>=1.3,<1.4",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
